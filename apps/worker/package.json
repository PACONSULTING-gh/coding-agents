{
  "name": "@coord/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc -p test/tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@coord/core": "workspace:*",
    "@coord/queue": "workspace:*",
    "@coord/db": "workspace:*",
    "@coord/github": "workspace:*",
    "@coord/graph": "workspace:*",
    "pino": "^10.3.1",
    "@coord/agents": "workspace:*"
  },
  "devDependencies": {
    "@testcontainers/postgresql": "^12.1.0",
    "pino-pretty": "^13.1.3",
    "testcontainers": "^12.1.0"
  }
}
